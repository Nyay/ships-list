import styled from '@emotion/styled'

export const ButtonTextStyled = styled.p`
  margin: 0;
  z-index: 1;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 100%;
  position: absolute;
`
