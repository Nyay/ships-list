import styled from '@emotion/styled'
import { fontSizeVariables } from '../../../../../../constants'
const { fontSizeXLarge } = fontSizeVariables

export const ShipInfoNameStyled = styled.h2`
  margin: 0;
  font-size: ${fontSizeXLarge};
  text-transform: uppercase;
`
