import styled from '@emotion/styled'
import { colorsVariables } from '../../../../../../constants'

const { unselectedWhite } = colorsVariables

export const ClassFilterHeaderStyled = styled.div`
  margin: 10px 0 -5px 27px;
  color: ${unselectedWhite};
`
